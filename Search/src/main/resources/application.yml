spring:
   application:
      name: Search-Service
     
server:
  port: ${PORT:8084}
  
  
eureka:
    client:
        serviceUrl:
            defaultZone: http://172.20.10.2:8761/eureka




    instance:
        prefer-ip-address: true
        ip-address: 172.20.10.2
        instance-id: Search-3
        metadataMap:
          zone: zone3


endpoints:
  shutdown:
    enabled: true
    sensitive: false