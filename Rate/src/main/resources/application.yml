spring:
   application:
      name: Rating-Service
   rabbitmq:
     host: localhost
     port: 5672
     username: guest
     password: guest
     
server:
  port: ${PORT:8099}
  
  
eureka:
    client:
        serviceUrl:
            defaultZone: http://localhost:8761/eureka
    instance:
      prefer-ip-address: true
      ip-address: 192.168.43.81
      instance-id: Rate-1
      metadataMap:
            zone: zone1
  

endpoints:
   shutdown:
      enabled: true
      sensitive: false

  

  